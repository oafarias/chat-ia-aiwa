services:
  web-aiwa:
    build: .
    container_name: aiwa-chat-web
    restart: always
    ports:
      - "8070:8000"
    volumes:
      - ./db.sqlite3:/app/db.sqlite3
      - ./staticfiles:/app/staticfiles
    environment:
      - DEBUG=False
      - DJANGO_ENV=prod
    networks:
      - aiwa-network
    env_file:
      - .env

networks:
  aiwa-network:
    driver: bridge